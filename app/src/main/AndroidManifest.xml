<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ampr.counter_strikecallouts" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="ampr.counter_strikecallouts.SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name="ampr.counter_strikecallouts.SelectMap"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name="ampr.counter_strikecallouts.RunDotTest"
            android:label="@string/app_name">
            android:theme="@android:style/Theme.NoTitleBar">
        </activity>

        <activity
            android:name="ampr.counter_strikecallouts.Countdown"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name="ampr.counter_strikecallouts.Result"
            android:label="@string/app_name">

        </activity>

    </application>

</manifest>
